# 웹 리포트 뷰어 시각 디자인

## Context

`web/`의 React 뷰어는 저장된 리포트를 사람이 읽는 화면이다. 기능 요구사항은 `SPEC.md`의 「웹 리포트 뷰어」절에 있고, 이 문서는 그 화면의 시각 결정만 다룬다. 구현은 `web/src/styles.css` 한 파일이며, 여기 적힌 결정을 토큰으로 옮겨 담았다.

목표는 읽기다. 리포트 본문과 근거가 주인공이고 장식은 그 뒤에 선다.

## 무엇을 따랐는가

Apple의 디자인 언어에서 다음을 가져왔다. 분위기만 흉내내지 않고 수치까지 맞춘 항목을 적는다.

| 항목 | 적용 |
|---|---|
| 본문 크기 | 17px. Apple이 iOS와 apple.com에서 쓰는 본문 크기다. 14~16px보다 장문을 읽기 편하다 |
| 표제 자간 | 큰 글자일수록 자간을 좁힌다. 표제 `-0.025em`, 소제목 `-0.018em`, 본문 `-0.01em` |
| 색 | 순수 검정·흰색을 쓰지 않는다. 본문은 `#1d1d1f`, 배경은 흰색이 아닌 `#fafafc` |
| 라운드 | 판은 18px, 안쪽 상자는 12px, 버튼은 알약(`980px`) |
| 재질 | 내비는 반투명에 `backdrop-filter: saturate(180%) blur(20px)`. 채도를 올리지 않으면 뒤 색이 탁해진다 |
| 이징 | `cubic-bezier(0.25, 0.1, 0.25, 1)`, 260ms. 시작이 부드럽고 끝이 차분하다 |
| 묶음 표현 | iOS 설정 화면처럼 항목을 둥근 판 하나에 담고 안에서 얇은 선으로 나눈다. 항목마다 카드를 두지 않는다 |
| 경계 | 선보다 색의 차이로 나눈다. 선을 쓸 때는 `rgba(0,0,0,0.08)` 수준으로 얇게 |
| 수치 정렬 | 표의 숫자 열은 `font-variant-numeric: tabular-nums`로 자릿수 폭을 고정하고 오른쪽에 맞춘다 |

## 표

참고 논문의 지표는 HTML `<table>`로 그린다. 연도·인용수·오픈액세스는 논문끼리 나란히 놓고 비교하는 값이라, 항목마다 배지를 늘어놓는 나열식보다 표가 빠르게 읽힌다.

표에는 색을 아끼기로 했다. 행이 여러 개인데 한 행에 파스텔 배지가 셋씩 들어가면 표가 소란해지고 정작 제목이 눌린다. 그래서 연도와 인용수는 색 없는 숫자로 두고, 파스텔 배지는 오픈액세스 여부에만 남겼다. 제목 열이 남는 폭을 가져가고 지표 열은 내용만큼만 차지한다.

좁은 화면에서는 표만 가로로 스크롤한다. 페이지 본문까지 함께 밀리면 읽던 자리를 잃기 때문이다.

## 파스텔 적용 규칙

파스텔은 색조를 넣되 읽기를 방해하지 않아야 한다. 규칙을 둘로 정했다.

**파스텔은 배경에만 쓴다.** 글자까지 연하게 하면 읽히지 않는다. 본문 글자는 라이트/다크 모두 기본 색을 유지한다.

**파스텔 배경에는 같은 색조의 진한 글자를 짝지어 정의한다.** `--mint-bg`와 `--mint-fg`처럼 쌍으로 두고 따로 쓰지 않는다. 파스텔 위에 파스텔 글자를 얹으면 명암비가 무너지기 때문이다.

색조는 다섯 가지다. 다섯을 고른 이유는 배지의 종류(연도·인용·오픈액세스)를 색으로 구분하고, 목록 카드와 근거 번호를 순환시켜도 같은 색이 바로 옆에 오지 않게 하기 위함이다.

| 이름 | 라이트 배경 | 라이트 글자 | 쓰임 |
|---|---|---|---|
| lavender | `#efeafe` | `#6247bd` | 근거 번호 기본, 구획 개수, 인라인 코드 |
| mint | `#e0f5ec` | `#0e7a57` | 오픈액세스 배지 |
| peach | `#ffede6` | `#b04a22` | 배경 워시, 순환 |
| sky | `#e5f1fe` | `#1264c0` | 발행 연도 배지 |
| butter | `#fdf3dc` | `#8a5a12` | 인용수 배지 |

배경에는 이 색들을 옅은 방사형 그라디언트 셋으로 겹쳐 깐다. 한 색으로 읽히지 않게 서로 다른 위치에 두고, `background-attachment: fixed`로 고정해 스크롤할 때 따라 움직이지 않게 했다.

강조색은 파스텔 계열의 페리윙클 `#4f5bd5`다. Apple의 `#0071e3`보다 부드럽지만, 링크로 읽히도록 흰 배경에서 5.54:1까지 눌렀다.

## 타입 스케일

| 역할 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 페이지 표제 | `clamp(2rem, 5vw, 2.75rem)` | 600 | `-0.025em` |
| 표제 부설명 | 1.1875rem (19px) | 400 | `-0.012em` |
| 구획 제목 | 1.375rem (22px) | 600 | `-0.018em` |
| 카드 제목 | 1.3125rem (21px) | 600 | `-0.018em` |
| 본문 | 1.0625rem (17px) | 400 | `-0.01em` |
| 부가 설명 | 0.9375rem (15px) | 400 | `-0.01em` |
| 메타·배지 | 0.875 / 0.8125rem (14/13px) | 400~600 | `0` |

작은 글자는 자간을 좁히지 않는다. 좁히면 오히려 붙어 읽기 어려워진다.

## 폰트

웹폰트를 쓰지 않는다. Apple 자신도 SF Pro를 시스템 폰트로 배포하므로 시스템 폰트를 쓰는 것이 원본에 더 가깝고, 이 뷰어는 로컬 도구라 오프라인에서도 같게 보여야 한다.

```text
-apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
"Segoe UI Variable Display", "Segoe UI", system-ui, "Apple SD Gothic Neo",
"Pretendard", "Malgun Gothic", sans-serif
```

macOS는 SF Pro를 직접 받는다. Windows 11에는 SF Pro가 없으므로 `Segoe UI Variable Display`가 받는데, 기하학적이고 자간이 좁아 SF Pro와 성격이 가깝다. 한글은 폰트 폴백이 글리프 단위로 동작하므로 앞쪽 라틴 서체가 한글 글리프를 갖지 않으면 자동으로 뒤쪽 한글 서체(macOS는 Apple SD Gothic Neo, Windows는 Malgun Gothic)가 받는다. `Pretendard`는 설치되어 있으면 쓰고 없으면 건너뛴다.

## 다크 모드

`prefers-color-scheme`으로 전환한다. 토글은 두지 않았다 — 뷰어에 설정 화면이 없고, OS 설정을 따르는 것이 이 화면의 유일한 사용자에게 맞다.

어두운 쪽에서는 세 가지를 바꾼다.

- 배경을 순수 검정 대신 살짝 푸른 `#0a0a0f`로 둔다. 파스텔 워시가 순수 검정 위에서는 탁해진다.
- 파스텔을 불투명 색이 아니라 낮은 투명도(0.16~0.18)의 물감처럼 깔고, 글자를 밝은 파스텔로 뒤집는다.
- 그림자를 없앤다. 어두운 배경에서 그림자는 보이지 않고, 표면의 밝기 차이가 그 역할을 대신한다.

## 접근성

`web/src/styles.css`의 색 조합을 WCAG 명암비로 실측했다. 배지 글자가 13px이라 AA 기준 4.5:1을 적용했다.

| 조합 | 라이트 | 다크 |
|---|---|---|
| lavender | 5.66 | 7.49 |
| mint | 4.68 | 8.36 |
| peach | 4.81 | 7.90 |
| sky | 5.08 | 8.00 |
| butter | 5.36 | 8.29 |
| 강조색·링크 | 5.54 | 7.82 |
| 본문 | 16.83 | 16.39 |
| 부가 설명 | 5.95 | 7.14 |

가장 낮은 조합이 4.68:1로 AA를 넘긴다. 3차 색(`--text-tertiary`)은 시각적 장식에만 쓰므로 3:1 기준으로 보았고 라이트 3.42, 다크 4.95다.

그 밖에:

- 초점 링은 `:focus-visible`로 키보드 사용자에게만 보여준다. 마우스 클릭에는 나타나지 않는다.
- `prefers-reduced-motion: reduce`에서 전환과 애니메이션을 끄고 부드러운 스크롤도 해제한다.
- 카드의 갈매기표와 돌아가기 버튼의 화살표는 장식이라 `aria-hidden`으로 감춘다.
- 색만으로 뜻을 전하지 않는다. 배지는 색과 함께 글자로도 종류를 밝힌다.

## 하지 않은 것

- **UI 프레임워크와 CSS 라이브러리를 쓰지 않는다.** 화면이 목록과 상세 둘뿐이라 도입할 이유가 없다. `SPEC.md`의 제약과 같다.
- **테마 토글을 두지 않는다.** OS 설정을 따른다.
- **본문 마크다운을 렌더링하지 않는다.** `save_report`의 본문은 산문으로 쓰고 문단은 빈 줄로 나누기로 계약을 정했다. 뷰어는 그 빈 줄을 문단 경계로 보아 문단마다 나눠 그리고, 문단 안의 줄바꿈은 살린다. 파서를 들이면 의존성이 늘고, 산문에는 표나 목록이 필요하지 않아 아직 그 값이 확인되지 않았다.
- **애니메이션을 화면 진입에 쓰지 않는다.** 목록이 스르륵 나타나는 연출은 읽기를 늦춘다. 움직임은 조작에 대한 반응(호버·펼침)에만 둔다.

## 검증

- `npm run build`로 컴파일을 확인한다.
- 색 조합의 명암비는 위 표대로 계산해 확인했다.
- CSS 선택자와 JSX의 `className`을 양방향으로 맞춰, 규칙 없는 클래스와 쓰이지 않는 규칙이 없음을 확인했다.
- 브라우저에서의 실제 렌더링은 사람이 눈으로 확인해야 한다. 자동 검증 대상이 아니다.
